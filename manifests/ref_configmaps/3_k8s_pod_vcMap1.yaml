apiVersion: v1
kind: Pod
metadata:
  name: pod1
  namespace: ts
  labels:
    app: pod1
spec:
  containers:
    - name: tomc
      image: heartocean/utils:live
      volumeMounts:
        - name: cmap-vol1
          mountPath: /opt

  volumes:
    - name: cmap-vol1
      configMap:
        name: cmap1
